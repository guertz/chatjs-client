namespace Navbar {
    namespace Profile {
        void RegisterLink();
        void EraseLink();

        namespace Events {
            void Disconnect(const char*);
            void SetText(const char*);
        }


    }
}